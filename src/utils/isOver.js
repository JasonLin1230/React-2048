// import handleKeyDown from "./handleMove"
// import {get_vacant} from "./getTiles"

export default function is_over(board){
    // if(get_vacant(board).length < 1){//棋盘已满
    //     var copy_board=Object.assign([],board);
    //     var new_board = [37,38,39,40].reduce(function(total, direction){
    //         return handleKeyDown(total, direction);
    //     }, copy_board);
    //     if(get_vacant(new_board).length > 0){
    //         return false;
    //     }else{
    //         return true;
    //     }
    // }else{
    //     return false;
    // }
}